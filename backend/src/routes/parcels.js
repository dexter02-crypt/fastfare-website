import express from 'express';
const router = express.Router();

// Stub: Parcels routes — to be implemented
router.get('/', (req, res) => {
    res.json({ message: 'Parcels endpoint — stub' });
});

export default router;
