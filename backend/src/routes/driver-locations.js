import express from 'express';
const router = express.Router();

// Stub: Driver Locations routes — to be implemented
router.get('/', (req, res) => {
    res.json({ message: 'Driver locations endpoint — stub' });
});

export default router;
