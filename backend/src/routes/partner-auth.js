import express from 'express';
const router = express.Router();

// Stub: Partner Auth routes — to be implemented
router.get('/', (req, res) => {
    res.json({ message: 'Partner Auth endpoint — stub' });
});

export default router;
